package com.odont.service;

import com.odont.model.Domicilio;

import java.util.List;

public class DomicilioService {

    /*private RepositoryGenerics<Domicilio> domicilioRepositoryGenerics;

    public DomicilioService(RepositoryGenerics<Domicilio> domicilioRepositoryGenerics) {
        this.domicilioRepositoryGenerics = domicilioRepositoryGenerics;
    }

    Domicilio guardar( Domicilio domicilio){
        // TODO: Validaciones
        domicilioRepositoryGenerics.guardar(domicilio);
        return domicilio;
    }

    Domicilio listar (Long id){
        return domicilioRepositoryGenerics.listar(id);
    }

    void eliminar(Long id){
        // TODO: Validaciones
        domicilioRepositoryGenerics.eliminar(id);
    }

    List<Domicilio> buscarTodos(){
        return domicilioRepositoryGenerics.buscarTodos();
    }

    Domicilio actualizar( Domicilio domicilio){
        // TODO: Validaciones
        return domicilioRepositoryGenerics.actualizar(domicilio);
    }

        //Validaciones primero: si el id existe en la tabla eliminarlo


     */
}
